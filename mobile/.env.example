# Point the app at the BFF backend (NestJS), which fronts Directus.
# Simulator can use localhost; a physical device must use your Mac's LAN IP
# (e.g. http://192.168.1.20:3000) so the phone can reach the backend.
EXPO_PUBLIC_API_URL=http://localhost:3000
